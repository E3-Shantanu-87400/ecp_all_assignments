#include<stdio.h>
int main()
{
	int a;
       printf("Enter the number:");
       scanf("%d",&a);
       printf("decimal form is=%d\n octal form is=%o\n hexadecimal form is=%x\n character is=%c",a,a,a,a);
       
       return 0;
}
